# excel and csv
numpy
xlrd~=2.0.1
pandas~=2.2.3
# pdf statement parsing
pdfplumber~=0.11.10
# postgres
psycopg2-binary~=2.9.10
# environment variables
dotenv~=0.9.9
# flask framework
Flask~=3.1.0
Flask-SQLAlchemy
Werkzeug
flask_cors
requests
# unit testing
pytest~=8.3.0
pytest-cov~=6.0.0
# test fixture generation only (scripts/generate_dummy_statement_fixtures.py) -
# not imported by the running app; xlrd (already above) only reads .xls, it
# can't write one, and there's no xlsx-writer in the app's own dependencies.
xlwt~=1.3.0
openpyxl~=3.1.5
reportlab~=5.0.1