# finance-manager-ui

view accounts, transactions, statistics, etc. Categorize transactions

## open ios app in xcode

1. ionic build Builds web code
2. npx cap add ios Adds native iOS project
3. npx cap copy / sync Copies web assets to iOS project
4. npx cap open ios Opens Xcode
5. chmod -R 777 ios


ionic cap open ios