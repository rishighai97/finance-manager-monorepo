# Placeholder - finance-manager-ui's own environment.prod.ts currently points
# at localhost too (no distinct prod deployment host exists yet in this repo).
# Update these once a real prod deployment exists. Running this suite's
# UI/backend tests against a real prod is a deliberate, risky action (signup,
# category, etc. tests write real data) - don't point --spring.profiles.active
# at prod without knowing that.
test-automation.account-service-url=http://localhost:5003
test-automation.api-gateway-url=http://localhost:5001
test-automation.transaction-service-url=http://localhost:5004
test-automation.statement-loader-url=http://localhost:5002
